// Una funcion simple que suma
function suma(a, b) {
  return a + b;
}

// Exportamos la funcion para poder probarla
module.exports = suma;

// Un pequeño console log para ver que funciona
console.log("La suma de 2 + 3 es:", suma(2, 3));
